[project]
name = "agent-flow-docs"
version = "0.1.0"
description = "Documentation for Agent Flow plugin"
requires-python = ">=3.12"
dependencies = [
    "mkdocs-material>=9.5",
    "pymdown-extensions>=10.0",
]
